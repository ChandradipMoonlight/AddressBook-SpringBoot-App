spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${db_url}
    username: ${db_user}
    password: ${db_pass}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update